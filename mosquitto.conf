# Mosquitto MQTT Broker Configuration File
listener 1883
allow_anonymous true

# Optional: enable persistence so retained messages survive restarts
persistence true
persistence_location /mosquitto/data/

# Logging (stdout is also captured by Docker logs)
log_dest stdout
log_type error
log_type warning
log_type notice
log_type information
